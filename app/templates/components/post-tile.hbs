{{#link-to 'post' post.id}}<div class="post col-md-4" id="button">
  <h2>{{post.title}} - {{post.author}}</h2>
  <img src={{post.image}} alt={{post.title}} width="100%">
  <p>{{post.snippet}}</p>

</div>{{/link-to}}
