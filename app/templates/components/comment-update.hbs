{{#if updateCommentShowing}}
  <form>
    <div class="form-group">
      <label for="author">Name</label>
      {{input value=author id="author" required="required" placeholder=comment.author  class="form-control"}}
    </div>

    <div class="form-group">
      <label for="date">Date</label>
      {{input value=date id="date" type="date" placeholder=comment.date  class="form-control"}}
    </div>


    <div class="form-group">
      <label for="commentContent">Comment</label>
      {{input value=commentContent id="commentContent" placeholder=comment.commentContent  class="form-control"}}
    </div>

    <button{{action 'updateComment' comment}} class="btn btn-success">Submit Changes</button>
  </form>
{{else}}
<button class="btn btn-success" {{action 'updateCommentShow'}}>Edit</button>
{{/if}}
